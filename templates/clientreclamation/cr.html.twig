{% block tbody %}

    <div class="container">
    <div class="row">
        {% for reclamation in reclamations %}

        <div class="card" style="width:300px">

            <div class="card-body">
                <h4 class="card-title">{{ reclamation.id }}</h4>
                <p class="card-text">{{ reclamation.description }}</p>

                <a href="#" class="btn btn-primary">See Profile</a>
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}